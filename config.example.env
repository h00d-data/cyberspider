# NVD API KEY (optional but recommended)
NVD_API_KEY=YOUR_API_KEY_HERE


💡 Obs:
- nvdlib é opcional (o script já trata se não tiver API Key)
- matplotlib é só pra gráfico do relatório

👉 O usuário copia pra .env ou exporta direto no sistema:
export NVD_API_KEY=xxxxx
